// ...existing code...

// Ensure setSearchData is defined as a function
const setSearchData = (data) => {
    // Function implementation
};

// ...existing code...

// Usage of setSearchData
setTimeout(() => {
    setSearchData(data); // Ensure data is defined or passed correctly
}, 1000);

// ...existing code...
